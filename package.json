{
  "name": "@sagi.io/cfw-jwt",
  "version": "0.0.5",
  "description": "Generate JWTs on Cloudflare Workers using the WebCrypto API",
  "main": "index.js",
  "author": "Sagi Kedmi <git@sagi.io> (https://sagi.io)",
  "homepage": "https://sagi.io",
  "license": "MIT",
  "private": false,
  "scripts": {
    "build": "babel --ignore '**/*.test.js' --ignore testdata src -d lib --verbose",
    "prepublish": "yarn build",
    "coverage": "yarn build && yarn jest --coverage",
    "test": "yarn build && yarn jest"
  },
  "dependencies": {
    "base64url": "^3.0.1"
  },
  "devDependencies": {
    "@babel/cli": "^7.4.4",
    "@babel/core": "^7.4.5",
    "@babel/preset-env": "^7.4.5",
    "coveralls": "^3.0.3",
    "jest": "^24.8.0",
    "jest-junit": "^6.4.0",
    "node-webcrypto-ossl": "^1.0.44",
    "prettier": "^1.17.0"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/sagi/cfw-jwt.git"
  }
}
